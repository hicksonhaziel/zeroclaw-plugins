[workspace]
members = [
    "crates/mandate-core",
    "plugins/realms-execution-audit",
    "plugins/realms-vote-build",
]
resolver = "2"

[profile.release]
opt-level = "s"
lto = true
strip = true
codegen-units = 1
