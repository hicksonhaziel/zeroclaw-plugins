name = "realms-execution-audit"
version = "0.1.0"
description = "T0 read-only Phase 1 capability scaffold for the future Mandate Realms execution auditor"
author = "Mandate contributors"
wasm_path = "realms_execution_audit.wasm"
capabilities = ["tool"]
# T0/read-only: jailed plugin config plus outbound HTTPS, and nothing else.
permissions = ["config_read", "http_client"]
